import State from './Model/state';

class ExistFinallyOperator {

    // example: Array<ReachableState> = [];
    // todo: Array<ReachableState> = []
    // done: Array<ReachableState> = [];

    // find(start: ReachableState, predicate: Function): boolean {

    //     this.todo.push(start);

    //     while (this.todo.length > 0) {
    //         let currentState: ReachableState = this.todo.slice(0);

    //         this.done.push(currentState);

    //         foudit: Boolean = predicate.

    //         if (predicate(currentState)) {
    //           // construct the example path

    //           return true;
    //         }

    //         //go for successors
    //         for(let next of currentState.successors()) {
    //             if(!this.done.includes(next)) {
    //                 this.todo.push(next);
    //             }
    //         }
    //     }

    //     return true;

    // }

}
export default ExistFinallyOperator;